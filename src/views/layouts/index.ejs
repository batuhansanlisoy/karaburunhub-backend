<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %> | KaraburunGO</title>

    <!-- Google Font -->
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;600;700&display=swap" rel="stylesheet">

    <!-- Bootstrap & Icons -->
    <link rel="stylesheet" href="/vendor/bootstrap/css/bootstrap.min.css">
    <link rel="stylesheet" href="/vendor/bootstrap-icons/bootstrap-icons.css">

    <style>
        body {
            font-family: 'Poppins', sans-serif;
            background-color: white;
            color: #212121;
        }

        /* Sidebar */
        .sidebar {
            background-color: #1e1e1e;
            min-height: 100vh;
        }

        .nav-link.active {
            background-color: #0d6efd !important;
            color: #fff !important;
        }

        .nav-link:hover {
            background-color: #495057;
            color: #fff !important;
        }

        /* Scroll bar ufak stil */
        ::-webkit-scrollbar {
            width: 8px;
        }

        ::-webkit-scrollbar-thumb {
            background-color: rgba(0, 0, 0, 0.2);
            border-radius: 4px;
        }
    </style>
</head>
<body>
    <div class="container-fluid">
        <div class="row">
            <%- include('./navbar.ejs') %>
            <!-- Main Content -->
            <main class="col-12 col-md-9 col-lg-10 p-4" style="background-color: #e6e6e69d;">
                <%- body %>
            </main>

            <footer class="text-white text-center py-1" style="background-color: #1e1e1e;">
                <p class="mb-0">&copy; <%= new Date().getFullYear() %> KaraburunGO. Tüm hakları saklıdır.</p>
            </footer>
        </div>
    </div>
    <script src="/vendor/bootstrap/js/bootstrap.bundle.min.js"></script>
</body>
</html>
